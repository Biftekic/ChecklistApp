import { describe, it, expect, beforeEach } from "vitest";

describe("QAEngine", () => {
  describe("Question Flow", () => {
    it.todo("should start with service type question");
    it.todo("should progress to property type after service selection");
  });
});
